import React, { useState, useEffect } from "react";

const Optimize = () => {
  const [count, setCount] = useState(0);

  return <div style={{ padding: 30 }}>Optimize</div>;
};

export default Optimize;
